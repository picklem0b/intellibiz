// ─── intellibiz/db — Pure SQL engine, tenancy injection, escape hatches ──────
export { sql, db, getGovernanceLog, clearGovernanceLog } from '@intellibiz/db'
export type { SqlQuery, SqlFragment, GovernanceRecord, SudoBuilder } from '@intellibiz/db'
